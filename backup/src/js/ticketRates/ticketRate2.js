function ticketRate2() {
  if (speedLimit === 30) {
    if (drivingSpeed > speedLimit && drivingSpeed < 1.2*speedLimit) {
      ticketFine = 1000;
    } else if (drivingSpeed >= 1.2*speedLimit && drivingSpeed < 1.29*speedLimit) {
      ticketFine = 1500;
    } else if (drivingSpeed >= 1.3*speedLimit && drivingSpeed < 1.39*speedLimit) {
      ticketFine = 2000;
    } else if (drivingSpeed >= 1.4*speedLimit && drivingSpeed < 1.49*speedLimit) {
      ticketFine = 2500;
    } else if (drivingSpeed >= 1.5*speedLimit && drivingSpeed < 1.59*speedLimit) {
      ticketFine = 3000;
    } else if (drivingSpeed >= 1.6*speedLimit && drivingSpeed < 1.69*speedLimit) {
      ticketFine = 3500;
    } else if (drivingSpeed >= 1.7*speedLimit && drivingSpeed < 1.79*speedLimit) {
      ticketFine = 4500;
    } else if (drivingSpeed >= 1.8*speedLimit && drivingSpeed < 1.89*speedLimit) {
      ticketFine = 5000;
    } else if (drivingSpeed >= 1.9*speedLimit && drivingSpeed < 1.99*speedLimit) {
      ticketFine = 6500;
    } else if (drivingSpeed >= 2*speedLimit) {
      ticketFine = 7500;
    }

  } else

  if (speedLimit === 40) {
    if (drivingSpeed > speedLimit && drivingSpeed < 1.2*speedLimit) {
      ticketFine = 1000;
    } else if (drivingSpeed >= 1.2*speedLimit && drivingSpeed < 1.29*speedLimit) {
      ticketFine = 1500;
    } else if (drivingSpeed >= 1.3*speedLimit && drivingSpeed < 1.39*speedLimit) {
      ticketFine = 2000;
    } else if (drivingSpeed >= 1.4*speedLimit && drivingSpeed < 1.49*speedLimit) {
      ticketFine = 2500;
    } else if (drivingSpeed >= 1.5*speedLimit && drivingSpeed < 1.59*speedLimit) {
      ticketFine = 3000;
    } else if (drivingSpeed >= 1.6*speedLimit && drivingSpeed < 1.69*speedLimit) {
      ticketFine = 3500;
    } else if (drivingSpeed >= 1.7*speedLimit && drivingSpeed < 1.79*speedLimit) {
      ticketFine = 4500;
    } else if (drivingSpeed >= 1.8*speedLimit && drivingSpeed < 1.89*speedLimit) {
      ticketFine = 5000;
    } else if (drivingSpeed >= 1.9*speedLimit && drivingSpeed < 1.99*speedLimit) {
      ticketFine = 6500;
    } else if (drivingSpeed >= 2*speedLimit) {
      ticketFine = 7500;
    }

  } else

  if (speedLimit === 45) {
    if (drivingSpeed > speedLimit && drivingSpeed < 1.2*speedLimit) {
      ticketFine = 1000;
    } else if (drivingSpeed >= 1.2*speedLimit && drivingSpeed < 1.29*speedLimit) {
      ticketFine = 1500;
    } else if (drivingSpeed >= 1.3*speedLimit && drivingSpeed < 1.39*speedLimit) {
      ticketFine = 2000;
    } else if (drivingSpeed >= 1.4*speedLimit && drivingSpeed < 1.49*speedLimit) {
      ticketFine = 2500;
    } else if (drivingSpeed >= 1.5*speedLimit && drivingSpeed < 1.59*speedLimit) {
      ticketFine = 3000;
    } else if (drivingSpeed >= 1.6*speedLimit && drivingSpeed < 1.69*speedLimit) {
      ticketFine = 3500;
    } else if (drivingSpeed >= 1.7*speedLimit && drivingSpeed < 1.79*speedLimit) {
      ticketFine = 4500;
    } else if (drivingSpeed >= 1.8*speedLimit && drivingSpeed < 1.89*speedLimit) {
      ticketFine = 5000;
    } else if (drivingSpeed >= 1.9*speedLimit && drivingSpeed < 1.99*speedLimit) {
      ticketFine = 6500;
    } else if (drivingSpeed >= 2*speedLimit) {
      ticketFine = 7500;
    }

  } else

  if (speedLimit === 50) {
    if (drivingSpeed > speedLimit && drivingSpeed < 1.2*speedLimit) {
      ticketFine = 1000;
    } else if (drivingSpeed >= 1.2*speedLimit && drivingSpeed < 1.29*speedLimit) {
      ticketFine = 1500;
    } else if (drivingSpeed >= 1.3*speedLimit && drivingSpeed < 1.39*speedLimit) {
      ticketFine = 2000;
    } else if (drivingSpeed >= 1.4*speedLimit && drivingSpeed < 1.49*speedLimit) {
      ticketFine = 2500;
    } else if (drivingSpeed >= 1.5*speedLimit && drivingSpeed < 1.59*speedLimit) {
      ticketFine = 3000;
    } else if (drivingSpeed >= 1.6*speedLimit && drivingSpeed < 1.69*speedLimit) {
      ticketFine = 3500;
    } else if (drivingSpeed >= 1.7*speedLimit && drivingSpeed < 1.79*speedLimit) {
      ticketFine = 4500;
    } else if (drivingSpeed >= 1.8*speedLimit && drivingSpeed < 1.89*speedLimit) {
      ticketFine = 5000;
    } else if (drivingSpeed >= 1.9*speedLimit && drivingSpeed < 1.99*speedLimit) {
      ticketFine = 6500;
    } else if (drivingSpeed >= 2*speedLimit) {
      ticketFine = 7500;
    }

  } else

  if (speedLimit === 60) {
    if (drivingSpeed > speedLimit && drivingSpeed < 1.2*speedLimit) {
      ticketFine = 1000;
    } else if (drivingSpeed >= 1.2*speedLimit && drivingSpeed < 1.29*speedLimit) {
      ticketFine = 1500;
    } else if (drivingSpeed >= 1.3*speedLimit && drivingSpeed < 1.39*speedLimit) {
      ticketFine = 2000;
    } else if (drivingSpeed >= 1.4*speedLimit && drivingSpeed < 1.49*speedLimit) {
      ticketFine = 2500;
    } else if (drivingSpeed >= 1.5*speedLimit && drivingSpeed < 1.59*speedLimit) {
      ticketFine = 3000;
    } else if (drivingSpeed >= 1.6*speedLimit && drivingSpeed < 1.69*speedLimit) {
      ticketFine = 3500;
    } else if (drivingSpeed >= 1.7*speedLimit && drivingSpeed < 1.79*speedLimit) {
      ticketFine = 4500;
    } else if (drivingSpeed >= 1.8*speedLimit && drivingSpeed < 1.89*speedLimit) {
      ticketFine = 5000;
    } else if (drivingSpeed >= 1.9*speedLimit && drivingSpeed < 1.99*speedLimit) {
      ticketFine = 6500;
    } else if (drivingSpeed >= 2*speedLimit) {
      ticketFine = 7500;
    }

  } else

  if (speedLimit === 70) {
    if (drivingSpeed > speedLimit && drivingSpeed < 1.2*speedLimit) {
      ticketFine = 1000;
    } else if (drivingSpeed >= 1.2*speedLimit && drivingSpeed < 1.29*speedLimit) {
      ticketFine = 1500;
    } else if (drivingSpeed >= 1.3*speedLimit && drivingSpeed < 1.39*speedLimit) {
      ticketFine = 2000;
    } else if (drivingSpeed >= 1.4*speedLimit && drivingSpeed < 1.49*speedLimit) {
      ticketFine = 2500;
    } else if (drivingSpeed >= 1.5*speedLimit && drivingSpeed < 1.59*speedLimit) {
      ticketFine = 3000;
    } else if (drivingSpeed >= 1.6*speedLimit && drivingSpeed < 1.69*speedLimit) {
      ticketFine = 3500;
    } else if (drivingSpeed >= 1.7*speedLimit && drivingSpeed < 1.79*speedLimit) {
      ticketFine = 4500;
    } else if (drivingSpeed >= 1.8*speedLimit && drivingSpeed < 1.89*speedLimit) {
      ticketFine = 5000;
    } else if (drivingSpeed >= 1.9*speedLimit && drivingSpeed < 1.99*speedLimit) {
      ticketFine = 6500;
    } else if (drivingSpeed >= 2*speedLimit) {
      ticketFine = 7500;
    }

  } else

  if (speedLimit === 80) {
    if (drivingSpeed > speedLimit && drivingSpeed < 1.2*speedLimit) {
      ticketFine = 1000;
    } else if (drivingSpeed >= 1.2*speedLimit && drivingSpeed < 1.29*speedLimit) {
      ticketFine = 1500;
    } else if (drivingSpeed >= 1.3*speedLimit && drivingSpeed < 1.39*speedLimit) {
      ticketFine = 2000;
    } else if (drivingSpeed >= 1.4*speedLimit && drivingSpeed < 1.49*speedLimit) {
      ticketFine = 2500;
    } else if (drivingSpeed >= 1.5*speedLimit && drivingSpeed < 1.59*speedLimit) {
      ticketFine = 3000;
    } else if (drivingSpeed >= 1.6*speedLimit && drivingSpeed < 1.69*speedLimit) {
      ticketFine = 3500;
    } else if (drivingSpeed >= 1.7*speedLimit && drivingSpeed < 1.79*speedLimit) {
      ticketFine = 4500;
    } else if (drivingSpeed >= 1.8*speedLimit && drivingSpeed < 1.89*speedLimit) {
      ticketFine = 5000;
    } else if (drivingSpeed >= 1.9*speedLimit && drivingSpeed < 1.99*speedLimit) {
      ticketFine = 6500;
    } else if (drivingSpeed >= 2*speedLimit) {
      ticketFine = 7500;
    }

  } else

  if (speedLimit === 90) {
    if (drivingSpeed > speedLimit && drivingSpeed < 1.2*speedLimit) {
      ticketFine = 1000;
    } else if (drivingSpeed >= 1.2*speedLimit && drivingSpeed < 1.29*speedLimit) {
      ticketFine = 1500;
    } else if (drivingSpeed >= 1.3*speedLimit && drivingSpeed < 1.39*speedLimit) {
      ticketFine = 2000;
    } else if (drivingSpeed >= 1.4*speedLimit && drivingSpeed < 1.49*speedLimit) {
      ticketFine = 2500;
    } else if (drivingSpeed >= 1.5*speedLimit && drivingSpeed < 1.59*speedLimit) {
      ticketFine = 3000;
    } else if (drivingSpeed >= 1.6*speedLimit && drivingSpeed < 1.69*speedLimit) {
      ticketFine = 3500;
    } else if (drivingSpeed >= 1.7*speedLimit && drivingSpeed < 1.79*speedLimit) {
      ticketFine = 4500;
    } else if (drivingSpeed >= 1.8*speedLimit && drivingSpeed < 1.89*speedLimit) {
      ticketFine = 5000;
    } else if (drivingSpeed >= 1.9*speedLimit && drivingSpeed < 1.99*speedLimit) {
      ticketFine = 6500;
    } else if (drivingSpeed >= 2*speedLimit) {
      ticketFine = 7500;
    }

  } else {
    
  }
  console.log('ticketFine:', ticketFine);
};
